@model MongoDBPool.Models.Results

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm("Edit", "Result", FormMethod.Post, new { }))
{
    @Html.HiddenFor(model => model._id)

    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Results</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.HosBallsLeft)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.HosBallsLeft)
            @Html.ValidationMessageFor(model => model.HosBallsLeft)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.OppnetBallsLeft)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.OppnetBallsLeft)
            @Html.ValidationMessageFor(model => model.OppnetBallsLeft)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
